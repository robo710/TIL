- 조건형 반복문
    조건이 참인 경우 반복을 유지
    
    - while, do while
        ex:
        while(조건식){
        	실행문
        }
        
        while문은 조건식을 확인하고 실행문을 실행함
        ex:
        do
        {
        	실행문
        } while(조건식)        
        do while문은 실행문을 최소 한 번은 조건없이 do에서 실행문을 실행한 후 조건식을 확인한다
        
- 범위형 반복문
    반복 범위를 정해 반복을 수행
    
    - for
        // 0부터 9까지 동작 1씩 증가시키며 반복
        for(index로 사용할 변수 in 시작숫자..끝 숫자){
        	실행문
        }
        // step 뒤의 숫자만큼 증가시키며 반복
        for(index로 사용할 변수 in 시작숫자..끝 숫자 step 증가되는 숫자){
        	실행문
        }
        // 1씩 감소되며 반복, step으로 감소될 숫자 조정 가능
        	for(index로 사용할 변수 in 시작숫자 downTo 끝 숫자){
        	실행문
        }
        
        char 형식도 가능
