### 증가 연산자(increment operators)
++a, a++
변수의 값을 ‘1’ 증가시켜주는 역할을 한다.

### 감소 연산자(decrement operators)
—a, a—
변수의 값을 ‘1’감소시켜주는 역할

### 전위 연산자(prefix operators)
앞에 붙는 경우
연산자가 포함된 구문에서 이미 증감된 수를 반영하여 연산을 진행

### 후위 연산자(postfix operators)
증감된 수를 해당 구문에서 사용하지 않고 다음 구문부터 사용한다.
