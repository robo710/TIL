@Composable
inline fun Box(
    modifier: Modifier = Modifier,
    contentAlignment: Alignment = Alignment.TopStart,
    propagateMinConstraints: Boolean = false,
    content: @Composable BoxScope.() -> Unit
)

- modifier : layout에 적용할 Modifier
- contentAlignment : Box내부의 정렬 설정, 기본 = Alignment.TopStart
- propagateMinConstraints(false) : Box내부에 최소 제약 조건을 설정할지 여부
- content : Box내부의 내용
